# الرفع على GitHub من المتصفح — من غير أوامر

كله من الموقع مباشرة. مش محتاج git ولا Command Prompt.

---

## الجزء الأول: الملفات الـ12

**1.** افتح الريبو:
https://github.com/llimxillh2-del/aqar-monitor

**2.** فوق على اليمين: زرار **`Add file`** → **`Upload files`**

**3.** افتح مجلد `ارفع-دي-على-جيتهاب` على جهازك، **ظلّل الـ12 ملف** (Ctrl+A ثم شيل
   الملفين اللي أسماءهم عربي)، واسحبهم على شباك المتصفح.

   الملفات الـ12:
   ```
   monitor.py      render.py       sources.py      watcher.py
   quality.py      intel.py        bit_mzayasoft.py
   beit_alwatan.py config.py       bot.py
   selftest.py     README.md
   ```

**4.** تحت في خانة الوصف اكتب: `اصلاحات`

**5.** اضغط **`Commit changes`** الأخضر

> GitHub هيستبدل الملفات القديمة تلقائيًا — مش هيعمل نسخ مكررة.

---

## الجزء الثاني: ملف الـ workflow

ده في مجلد مخفي فمحتاج طريقة تانية — **تعديل مباشر على الموقع**:

**1.** افتح الرابط ده:
https://github.com/llimxillh2-del/aqar-monitor/edit/main/.github/workflows/monitor.yml

**2.** اضغط جوه المحرر واعمل **`Ctrl+A`** ثم **`Delete`** (امسح كل اللي فيه)

**3.** افتح الملف `monitor.yml نسخة احتياطية للصق.txt` اللي جنب الملف ده
   بالـ Notepad، اعمل **`Ctrl+A`** ثم **`Ctrl+C`**

**4.** ارجع للمتصفح واعمل **`Ctrl+V`**

**5.** فوق على اليمين: **`Commit changes...`** → **`Commit changes`**

---

## الجزء التالت: الأسرار

`Settings` → `Secrets and variables` → `Actions`

اضغط أيقونة القلم ✏️ جنب كل واحد وحط القيمة دي:

| السر | القيمة |
|---|---|
| `TELEGRAM_TOKEN` | `8953669973:AAElG4GV3mdD1q3_ooVS_d3lawwDStmtmKI` |
| `TELEGRAM_CHAT_ID` | `6336327254` |

وامسح `GEMINI_API_KEY` (مرفوض بـ 401).

---

## الجزء الرابع: شغّل

**1.** تبويب **`Actions`**
**2.** من الشمال: **`مرصد العقارات المصرية`**
**3.** فوق على اليمين: **`Run workflow`** → **`Run workflow`**
**4.** استنى 3-4 دقايق

---

## إزاي تعرف إنه ظبط

افتح التشغيلة → اضغط على خطوة **`فحص الإعدادات`**:

```
TELEGRAM_TOKEN   : 46 حرف  (المتوقع 46)
TELEGRAM_CHAT_ID : 10 حرف
```

وبعدين خطوة **`دورة كاملة`** — في آخرها:

```
[✓] تليجرام: 2 رسالة (3180 حرف)     ← ظبط، شوف تليجرام
[!] تليجرام: 0 نجحت · 2 فشلت         ← ابعتلي السطر
```

---

## ⚠️ متعملش دول

- **متسحبش** ملف `.env` — فيه التوكن
- **متسحبش** مجلد `state/` — دي ذاكرة النظام على GitHub
- **متسحبش** `index.html` أو `latest.json` — بيتولدوا تلقائيًا
- **متسحبش** ملف `اقرأني-أولاً.md` ده ولا ملف `.txt`
