# التعديلات — 4 أغسطس 2026

**13 ملف · 18 إصلاح · الفحص الذاتي: 27 نجح · 0 فشل**

---

## إزاي أرفعها

انسخ محتويات المجلد ده **فوق** ملفات المشروع (استبدال)، وبعدين:

```bash
cd "C:\Users\Ahmed Gaber\Desktop\aqar-monitor-main"
python selftest.py          # لازم يطلع 27 نجح · 0 فشل
git add -A
git commit -m "إصلاح الوقت والتكرار وتقسيم رسائل تليجرام"
git push
```

بعدها `Actions` → `Run workflow` → `once`.

> **مجلد `updated/` نفسه متترفعش** — امسحه بعد النسخ.

---

## 🔴 الرسايل ما كانتش بتوصل

**1. التقسيم كان بيكسر وسوم HTML** — `monitor.py` · `bot.py`
`<b>` كان بيتقسم على رسالتين، تليجرام يرفض بـ 400.
دلوقتي كل جزء بيتقفل وسومه ويعيد فتحها في اللي بعده.

**2. ترتيب الرسالة كان بيتقلب** — `monitor.py:_split_message`
السطر الطويل كان بيتحط قبل النص اللي قبله. اتظبط.

**3. الكود كان بيقول «مُرسلة» وهي فشلت** — `monitor.py`
دلوقتي بيقرا رد تليجرام: `[✓] تليجرام: 2 رسالة` أو `[!] 2 فشلت — راجع التوكن`.

**4. تنبيهات الرادار ماكانتش بتتبعت خالص** — `monitor.py`
`intel_msgs` كان بيتحسب وبيترمى. دلوقتي بيتبعت (أهم حاجة في النظام).

**5. حدّين مختلفين للطول** — `render.py`
كان بيبني 3800 و`monitor` يقسّم عند 3600 → إعادة تقسيم بتكسر الرسالة.
اتوحّدوا على 3400.

---

## 🟠 الوقت

**6. صيغ العدد العربي** — `render.py:ago()`

| كان | بقى |
|---|---|
| منذ 2 ساعة | منذ ساعتين |
| منذ 5 ساعة | منذ 5 ساعات |
| منذ 2 يومًا | منذ يومين |
| منذ 3 يومًا | منذ 3 أيام |

**7. التوقيت متثبّت على UTC+3** — `render.py` · `config.py`
مصر +3 صيفًا و+2 شتاءً. الموقع كان هيقدّم ساعة من نوفمبر لأبريل.
دلوقتي بيتحسب أوتوماتيك من تواريخ التوقيت الصيفي.

**8. موعد النهاردة كان بيظهر «مرّ»** — `beit_alwatan.py`
المقارنة كانت باللحظة بدل التاريخ.

---

## 🟡 الأخبار

**9. `&nbsp;&nbsp;` كانت بتظهر حرفيًا في كل خبر** — `sources.py`
`_strip_html` كان بيشيل الوسوم بس مش بيفك الرموز.

**10. الملخص كان تكرار للعنوان** — `sources.py`
ملخص Google News = العنوان + اسم الموقع. بيترمى لو مش بيضيف معلومة.

**11. الخبر الواحد في قسمين** — `sources.py`
منع التكرار كان جوه اللوب. بقى عام على كل الأقسام.

**12. الترتيب مش زمني** — `sources.py`
كان بالكلمات المفتاحية فالتواريخ بتقفز. بقى الأحدث أولاً.

**13. أي مصدر فيه «الوطن» بياخد Tier 1** — `quality.py`
`"الوطن"` و`"المال"` كـ substring. بقوا `"جريدة الوطن"` و`"جريدة المال"`.

---

## 🟢 البيانات

**14. التغييرات الرسمية مابتتحسبش «جديد»** — `watcher.py`
كلها كانت بتاخد نفس الرابط. دلوقتي بصمة المحتوى في الرابط.

**15. مصدر واقع من 12 دورة يتعرض «متقطع»** — `watcher.py`
3 فشل متتالي = «متعذّر».

**16. اقتطاع الذاكرة أبجدي** — `monitor.py` · `bit_mzayasoft.py`
`sorted(seen)[-4000:]` كان بيرمي روابط عشوائي فالتنبيهات تتكرر.
بقى ترتيب زمني.

**17. عدّاد المدن بيتراكم للأبد** — `beit_alwatan.py`
كان بيعد نفس الأخبار كل دورة.

**18. الخطوات العملية بتتطبع ليستة بايثون خام** — `render.py`
`['جهّز مستنداتك.', ...]` كانت ظاهرة على الموقع. بقت `<ol>`.

---

## ⚡ أسرع وأقصر

**الفحص السريع بقى موصّل** — `.github/workflows/monitor.yml`
كرون الساعة كان بيشغّل `--watch` (رسمي بس، وكله واقع) = صفر رسايل.
بقى `--fast-check`: أخبار بيت الوطن + إعلانات القطع الجديدة.
**تنبيه خلال ساعة بدل 4 ساعات.**

**الكرونين مابيتصادموش** — `5 */4` و `35 * * * *`

**النشر بقى الصفحات بس** — كان بيرفع الريبو كله (`.pyc` والحالة).

**فحص الأسرار** — خطوة بتطبع أطوال الأسرار وبتحذّر لو التوكن فيه `< >`.

**رسالة تليجرام أقصر** — الاستشراف اتشال منها (موجود على الموقع)،
والأخبار 3 لكل قسم بدل 4.

**الصفحة أقصر** — 12 عنصر لكل قسم بدل 18.

---

## ملفات جديدة

- **`selftest.py`** — 27 فحص يتأكدوا إن كل إصلاح شغال. شغّله بعد أي تعديل.
- **`README.md`** — كان فاضي.

---

## لسه محتاج منك

1. **`TELEGRAM_TOKEN`** — الـ workflow هيقولك طوله. لو مش 46، فيه `< >` أو مسافة.
2. **`GEMINI_API_KEY`** — مرفوض بـ 401. امسحه أو هات جديد.
3. **نصوص اليوتيوب و Reddit** محجوبين على GitHub — محتاجين تشغيل محلي.
